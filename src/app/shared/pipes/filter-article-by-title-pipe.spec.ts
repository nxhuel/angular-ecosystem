import { FilterArticleByTitlePipe } from './filter-article-by-title-pipe';

describe('FilterArticleByTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArticleByTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
